import styled from "styled-components";

export const Requests = styled.main`
    display: flex;
    width: 100%;
    background-color: rgb(244,244,244);
    .open--requests {
        width: 30%;
        box-shadow: 1px 0px 13px -4px var(--gray);
    }
`